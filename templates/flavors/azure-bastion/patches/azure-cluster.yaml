---
apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: AzureCluster
metadata:
  name: ${CLUSTER_NAME}
spec:
  bastionSpec:
    azureBastion:
      subnet:
        cidrBlocks:
          - 40.2.0.0/16
