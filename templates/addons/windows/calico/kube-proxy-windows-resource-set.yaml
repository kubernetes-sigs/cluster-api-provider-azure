apiVersion: addons.cluster.x-k8s.io/v1beta1
kind: ClusterResourceSet
metadata:
  name: crs-kube-proxy-windows
  namespace: default
spec:
  strategy: "ApplyOnce"
  clusterSelector:
    matchLabels:
      windows-service-proxy: kube-proxy
  resources:
    - name: kube-proxy-windows-addon
      kind: ConfigMap
