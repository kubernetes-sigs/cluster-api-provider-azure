---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: aromachinepools.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: AROMachinePool
    listKind: AROMachinePoolList
    plural: aromachinepools
    shortNames:
    - aromp
    singular: aromachinepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: MachinePool ready status
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Number of replicas
      jsonPath: .status.replicas
      name: Replicas
      type: integer
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: AROMachinePool is the Schema for the aromachinepools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AROMachinePoolSpec defines the desired state of AROMachinePool.
            properties:
              additionalTags:
                additionalProperties:
                  type: string
                description: AdditionalTags are user-defined tags to be added on the
                  underlying EC2 instances associated with this machine pool.
                type: object
              autoRepair:
                default: true
                description: |-
                  AutoRepair specifies whether health checks should be enabled for machines
                  in the NodePool. The default is true.
                type: boolean
              autoscaling:
                description: |-
                  Autoscaling specifies auto scaling behaviour for this MachinePool.
                  required if Replicas is not configured
                properties:
                  maxReplicas:
                    minimum: 1
                    type: integer
                  minReplicas:
                    minimum: 1
                    type: integer
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Labels specifies labels for the Kubernetes node objects
                type: object
              nodePoolName:
                description: |-
                  NodePoolName specifies the name of the nodepool in ARO
                  must be a valid DNS-1035 label, so it must consist of lower case alphanumeric and have a max length of 15 characters.
                maxLength: 15
                pattern: ^[a-z]([-a-z0-9]*[a-z0-9])?$
                type: string
                x-kubernetes-validations:
                - message: nodepoolName is immutable
                  rule: self == oldSelf
              platform:
                description: AROPlatformProfileMachinePool represents the NodePool
                  Azure platform configuration.
                properties:
                  availabilityZone:
                    description: AvailabilityZone specifying the availability zone
                      where instances of this machine pool should run.
                    type: string
                  diskSizeGiB:
                    description: DiskSizeGiB sets the disk volume size for the machine
                      pool, in Gib.
                    format: int32
                    type: integer
                  diskStorageAccountType:
                    description: |-
                      DiskStorageAccountType represents supported Azure storage account types.
                      Available values are Premium_LRS, StandardSSD_LRS and Standard_LRS.
                    type: string
                  subnet:
                    description: Azure subnet id
                    type: string
                  subnetRef:
                    description: Subnet Ref name that is used to create the VirtualNetworksSubnet
                      CR. The SubnetRef must be in the same namespace as the AroMachinePool
                      and cannot be set with Subnet.
                    type: string
                  vmSize:
                    description: VMSize sets the VM disk volume size to the node.
                    type: string
                type: object
              taints:
                description: Taints specifies the taints to apply to the nodes of
                  the machine pool
                items:
                  description: AROTaint represents a taint to be applied to a node.
                  properties:
                    effect:
                      description: |-
                        The effect of the taint on pods that do not tolerate the taint.
                        Valid effects are NoSchedule, PreferNoSchedule and NoExecute.
                      enum:
                      - NoSchedule
                      - PreferNoSchedule
                      - NoExecute
                      type: string
                    key:
                      description: The taint key to be applied to a node.
                      type: string
                    value:
                      description: The taint value corresponding to the taint key.
                      pattern: ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$
                      type: string
                  required:
                  - effect
                  - key
                  type: object
                type: array
              version:
                description: |-
                  Version specifies the OpenShift version of the nodes associated with this machinepool.
                  AROControlPlane version is used if not set.
                type: string
            required:
            - nodePoolName
            type: object
          status:
            description: AROMachinePoolStatus defines the observed state of AROMachinePool.
            properties:
              availableUpgrades:
                description: Available upgrades for the ARO MachinePool.
                items:
                  type: string
                type: array
              conditions:
                description: Conditions defines current service state of the managed
                  machine pool
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              failureMessage:
                description: |-
                  FailureMessage will be set in the event that there is a terminal problem
                  reconciling the state and will be set to a descriptive error message.

                  This field should not be set for transitive errors that a controller
                  faces that are expected to be fixed automatically over
                  time (like service outages), but instead indicate that something is
                  fundamentally wrong with the spec or the configuration of
                  the controller, and that manual intervention is required.
                type: string
              id:
                description: ID is the ID given by ARO.
                type: string
              provisioningState:
                description: |-
                  ProvisioningState represents the asynchronous provisioning state of an ARM resource.
                  Allowed values are; Succeeded, Failed, Canceled, Accepted, Deleting, Provisioning and Updating.
                type: string
              ready:
                default: false
                description: |-
                  Ready denotes that the AROMachinePool nodepool has joined
                  the cluster
                type: boolean
              replicas:
                description: Replicas is the most recently observed number of replicas.
                format: int32
                type: integer
              version:
                description: ARO-HCP OpenShift semantic version, for example "4.20.0".
                type: string
            required:
            - ready
            - version
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
