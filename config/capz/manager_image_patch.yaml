apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: gcr.io/palette-pcp-08fkpbtkx7xcwa7xn2/cluster-api-azure-controller-s390x:dev
        name: manager
